<div class="row">
    <aside class="col-md-4">
        <section class="user_info">
            <h1>
                <%= gravatar_for @user %>
                <%= @user.name %>
            </h1>        
        </section>
        <ul class="row profile-wrapper">
            <li>年齢：<%= @user.age %></li>
            <li>プロフィール：<%= @user.profile %></li>
            <li>趣味：<%= @user.hobby %></li>
        </ul>
        <% if(@user == current_user) %>
            <div>
                <%= link_to "edit profile", edit_user_path(current_user) %>
            </div>
        <% end %>
        <div>
            <%= link_to "followings", followings_path(@user.id) %>
        </div>
        <div>
            <%= link_to "followers", followers_path(@user.id) %>
        </div>
    </aside>
    <div class="col-md-8 followings-wrapper">
        <h2><%= @user.name %>のフォロー</h2>
        <ul>
            <% @user.following_users.each do |following| %>
                <li class="clearfix following">
                    <div class="avatar clearfix">
                        <%= gravatar_for following %>
                    </div>
                    <div class="name">
                        <%= following.name %>
                    </div>
                    <!--
                    <div id="follow_form">
                        <% if current_user != following %> 
                            <% if current_user.following?(following) %>
                                <%= 
                                    form_for(current_user.following_relationships.find_by(followed_id: following.id), html: {method: :delete}, remote: true) do |f| 
                                %>
                                <%= f.submit "UnFollow", class: "btn" %>
                                <% end %>
                            <% else %>
                                <%= form_for(current_user.following_relationships.build, remote: true) do |f| %>
                                    <div>
                                        <%= hidden_field_tag :followed_id, following.id %>
                                    </div>
                                    <%= f.submit "Follow", class: "btn btn-primary" %>
                                <% end %>
                            <% end %>
                        <% end %>
                    </div>
                    -->
                </li>
            <% end %>
        </ul>
    </div>
</div>